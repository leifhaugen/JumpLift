# JumpLift

This is PaperMC plugin that adds teleport-based elevators using a vanilla multi-block
structures.

## About

TODO

## Development

### Setup

TODO

### Releasing

```bash
vi pom.xml # update version
git add pom.xml
git commit -m "chore: version bump: v1.0.0"
git tag 1.0.0
git push origin master 1.0.0
# Download the artifact from the CI job
```
